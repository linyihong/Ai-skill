# Intake — Specification / Reference Lock

未鎖不得比較候選。Lock ≠ Identity Acceptance。

填：

- [`records/character-specification.yaml`](records/character-specification.yaml)
- [`records/reference-set.yaml`](records/reference-set.yaml)

`exploration` 可生成但不比較、不填 Identity Acceptance 為 accepted。

推進：兩份 lock 完整（見 [`records/artifact-gates.yaml`](records/artifact-gates.yaml) `specification_reference_lock`）。
失敗 rollback：`intake_or_spec_author`。
