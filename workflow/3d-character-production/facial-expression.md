# Facial Expression

僅當 `deformation_acceptance_set.decision == pass`。
本 stage 不定義 identity 規則。表情證據進入 runtime-ready pack 的
`expressions`／`expression_trigger` 欄（見 records）。

VRM blendshape 清單屬 Phase 4 profile，不在本檔展開。
