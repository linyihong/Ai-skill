# Candidate Generation

填 [`records/candidate-record.yaml`](records/candidate-record.yaml)。

- `decision` 與 `reason` 缺則不得 promotion。
- `rejected` 必須留存。
- `seed: unavailable` 與 `generation_settings.availability: partial` 合法；禁止硬補。

Acquisition strategy 可替換；不把生成服務寫進本檔。
